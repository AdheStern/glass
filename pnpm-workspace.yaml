# No hay monorepo: sin `packages:`. Este archivo solo lleva ajustes de pnpm.
# pnpm 11 usa `allowBuilds` (mapa) para autorizar scripts de instalación.
allowBuilds:
  "@prisma/client": true
  "@prisma/engines": true
  prisma: true
  esbuild: true
