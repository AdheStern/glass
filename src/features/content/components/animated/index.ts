// Re-exporta los componentes de animación (cliente) para que los bloques
// (server components) los rendericen sin importar `motion` directamente.
export { default as Reveal } from "./reveal";
export { default as StaggerGrid } from "./stagger-grid";
